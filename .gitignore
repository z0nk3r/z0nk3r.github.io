# Build output / caches
_site/
.jekyll-cache/

# Local dev tooling
vendor/
.bundle/

# Internal-only (kept local, not published)
SETUP.md
# Source-comment prose lifted out of the shipped files - reference material,
# never deployed. Also in .git/info/exclude and in _config.yml's exclude: list,
# so it cannot be committed by accident and Jekyll would not copy it anyway.
docs/

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
*.bak
*~

# Editor/IDE folders
.vscode/
.idea/

# Common JS tooling (none in use today, but this project has zero npm
# tooling per SETUP.md and the project notes - anything that shows up here later is
# almost certainly local scratch, not site source)
node_modules/
npm-debug.log*

# General log files
*.log
